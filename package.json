{
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "setup": "npm install",
    "dev": "concurrently \"npm run dev -w apps/server\" \"npm run dev -w apps/extension-is24\"",
    "build": "npm run build -w apps/extension-is24",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run check -w apps/extension-is24 && npm run typecheck -w apps/server",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "concurrently": "^9.0.0"
  }
}
